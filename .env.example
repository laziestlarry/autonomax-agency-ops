# Server
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://yourdomain.com
CORS_ORIGIN=https://yourdomain.com

# Database (SQLite for demo, PostgreSQL for production)
DATABASE_URL="file:./prod.db"

# Stripe (leave empty for simulation mode)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
